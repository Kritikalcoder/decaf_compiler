class Program {
	int A[10];
	int size;

	void maxmin () {
		int max, min;
		max = A[0];
		min = A[0];

		for i = 0, size {
			if (A[i] > max) {
				max = A[i];
			}
			if (A[i] < min) {
				min = A[i];
			}
		}
		callout("printf", "%d", max);
		callout("printf", "%d", min);
	}
	void main () {
		size = 10;
		for i = 0, size {
			A[i] = i;
		}
		
		maxmin();
	}
}